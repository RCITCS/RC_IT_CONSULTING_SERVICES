# Pages

Each public route or coherent route family owns an explicit named page module here. Page modules compose shared components; they do not own global navigation, footer, transport APIs or unrelated routes.

Phase 3 migrates the current renderer functions into this directory page by page while preserving the approved DOM, content and interactions.
