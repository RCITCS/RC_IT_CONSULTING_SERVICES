# API boundary

HTTP request handlers belong here. Handlers validate transport input, call services and map results to stable HTTP responses; they should not contain persistence or email-provider logic directly.
