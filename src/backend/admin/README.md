# Admin application

Private administration pages, admin-only components and operational workflows live here. The admin product is deployed separately from the public marketing bundle and is always protected by server-side authentication/authorization.
